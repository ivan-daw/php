Composer -> solo 1 front controller para todas las clases
si no se usa  -> require_once, src/controller en el front controller las 3 clases

require_once != use
php (se necesita para cargar las clases) 	java (hacer un new y carga la clase)

use = import
es para paquetes
new class()	new paquete.class()
new Hashmap	new util.Hashmap
sin usar new Controller\nombreclase
